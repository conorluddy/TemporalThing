{
  "name": "temporalthing",
  "version": "0.0.1",
  "description": "Temporal API thing",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "serve": "http-server -p 8080 -c-1"
  },
  "author": "conorluddy@gmail.com",
  "license": "MIT",
  "dependencies": {
    "@js-temporal/polyfill": "^0.4.4",
    "typescript": "^5.5.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "esbuild": "0.23.0",
    "http-server": "^14.1.1",
    "ts-node-dev": "^2.0.0"
  }
}
